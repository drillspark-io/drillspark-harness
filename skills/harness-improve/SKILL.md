---
name: harness-improve
description: 既にあるハーネスから処理の一覧を起こし、処理ごとの理想図をオーナーと対話しながら育て、実ファイルとの差分を出すまで。実装はしない。引き継いだハーネスにも自分で作ったハーネスにも入る。図も設計書も無い前提で始める。
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, mcp__drillspark__get_project, mcp__drillspark__list_projects, mcp__drillspark__list_diagrams, mcp__drillspark__get_diagram, mcp__drillspark__get_diagram_rules, mcp__drillspark__validate_diagram, mcp__drillspark__create_project, mcp__drillspark__update_diagram, mcp__drillspark__list_versions, mcp__drillspark__restore_version
---

# harness-improve — ハーネス全体を改善する

7つの工程のうち**〈改善する〉**。対応表は harness-implement と共有する
→ [MAPPING.md](../harness-implement/MAPPING.md)

```text
処理を洗い出して表にする →［承認①］→ 処理ごとに理想図を対話で育てる →［承認②］
  → 処理ごとの差分 → 全処理ぶん繰り返す → 横断差分 → 改善/<日付>.md に記録して終わる
```

## この工程の固有の仕事

**既存の実ファイルから契約を起こすこと。** `harness-implement` は**目的から**図を降ろす。
改善は**実ファイルから**図を起こす。成果物は同じ「図」でも、入力が逆を向いている。
この向きの手順を持っているのは改善だけで、だから引き継いだハーネスの入口になる。

**症状1つを直す工程ではない。** 直す対象を1枚に特定できるのは、図が既にあって
どこが壊れたか分かっている場合だけ。引き継いだ直後はその条件が揃わない。

### やらないこと

| | 誰の仕事か |
|---|---|
| **実ファイルを直す** | `harness-implement`〈実装する〉。改善は差分を作業指示として渡すだけ |
| **合格条件を決める** | `harness-implement`〈合格条件を決める〉 |
| **監査**（承認済みの図 ↔ 実ファイル） | 評価役。→ 末尾「監査との違い」 |
| **判定基準を動かす** | 改善は決めない。`reference/harness-design-criteria.md` は読むだけ |

## 前提 — 何も準備されていないところから始める

入口は2つある。**自分が作ったハーネス**と、**他人が作ったハーネスを引き継いだとき**。
**手順は分けない。違うのは「在るもの」だけ**で、それは0節で数えれば分かる。

| | 引き継いだとき | 自分で作ったとき |
|---|---|---|
| 図 | DrillSpark は作った人のアカウントにあり git の外。**読めないのが普通** | 在ることが多いが古い |
| `docs/harness/<名前>/設計.md` | 無い | 在るが実物とずれている |
| `.claude/rules/` `.claude/tests/` | 無い | 作った工程しだい |
| なぜそう作ったか | どこにも書かれていない | 覚えている。ただし**書かれていないことに変わりはない** |

> **分からない ≠ 欠陥。** 引き継いだハーネスには、こちらが知らない理由で置かれている部品がある。
> 意図が読めないものを「無駄」と判定しない。**一覧に載せてオーナーに聞く。**
> 自分で作ったハーネスでも、**覚えている理由は根拠にならない**。表に書いて初めて根拠になる。

**準備されていないものの中に、DrillSpark への接続だけは含めない。**
2節で理想図を作るので**必須**。0節を数える前に `mcp__drillspark__list_projects()` を1回だけ流し、
繋がらなければ**そこで止めて案内する** →
[`reference/drillspark-setup.md`](../../reference/drillspark-setup.md)。
1節の承認①を通してから詰まると、処理一覧の合意が宙に浮く。

## 0. 在るものを数える

一覧を作る材料であり、**どの節を書き換えてよいか**の判定でもある。

| 探すもの | 見るところ | 無いとき |
|---|---|---|
| 実ファイル | `.claude/{agents,skills,rules,tests}/`・`settings.json`・<br>`CLAUDE.md`・hooks が指すスクリプトの実体 | ここだけは必ず在る。**在るのがこれだけ、という状態があり得る** |
| 設計書 | `docs/harness/<ハーネス名>/` 一式（索引 `設計.md`・`処理/<処理名>/図.md`・<br>`合格条件.md`・`実装.md`・`統合.md`・`評価/<日付>.md`） | 5節で**索引を作る**（改善の主要な成果物） |
| 図 | `処理/<処理名>/図.md` のURL／索引のリンク／README／`.claude/` 内の DrillSpark URL | 2節で**新規に作る**。既存があっても他人のものは触らない |
| 凍結した合格条件 | `.claude/tests/`、`処理/<処理名>/合格条件.md` | **在るなら絶対に触らない**（5節） |

**自分で作ったハーネスでも数える。記憶で代用しない。** Glob と Read で済む。

## 1. 処理を洗い出して表にする

### 処理とは何か

**複数の skill・agent・hook・rules が総合して動く1つのワークフロー。**
ブログを書く、動画を作る、といった単位。

**skill を数えても処理の数にはならない。** このリポジトリ自身が反例で、
`harness-implement` は skill 1つだが中身は6工程、`harness-visualize` は skill 1つで処理1つ。
1:1 に見えるのは偶然で、あてにすると理想図が6工程分を1枚に押し込む形になり、
ノード数の上限で必ず破綻する。

**切り出す軸は「起動」。** 同じきっかけで動きだし、1つの成果物を出しきるまでが1処理。
部品の数ではなく、**流れの単位**で切る。

**起動点は grep で列挙する。記憶で列挙しない。** `settings.json` の hooks・permissions、
skill / agent の frontmatter、コマンド定義 — 機械で拾える起動点を先に全部並べてから切り出す。
ここで拾い損ねた起動が、4節の「処理一覧の穴」になる。検出より予防が安い。

### 表の列

`docs/harness/<ハーネス名>/改善/<日付>.md` に置き、索引 `設計.md` の処理一覧にも同じ行を作る。
**ここに無いものは評価が測れない。**

| 処理名 | 起動 | 使っている部品 | 成果物 | 何のために在るか | DrillSpark |
|---|---|---|---|---|---|

- **使っている部品** — skill / agent / hook / rules を実名で。
  **同じ部品が2つの処理に出てきたら、それが処理をまたぐ重複**で、4節の材料になる
- **DrillSpark** — 2節で作ったプロジェクトの URL。処理ごとに1つ。
  名前の規約は `<ハーネス名> / <処理名>`。**URL 列と名前規約の両方を持つ** —
  図が処理ごとに散っているので、表から辿れないと全体像が機能しない

### 「何のために在るか」だけは調べられない

**事実を調べるのは自分の仕事。** 処理名・起動・部品・成果物は実ファイルから調べる。
オーナーに聞かない。**調べられないのはこの1列だけ**で、ここを空のまま出す。

| 値 | 意味 |
|---|---|
| 散文1行 | この処理が在る理由 |
| **不要** | オーナーが要らないと判断した処理 |
| 空欄 | **まだ答えていない。承認が終わっていない** |

**「不要」を選択肢として先に見せる。** 人は理由を聞かれると後付けで作るので、
逃げ道が無いと惰性で在るだけの処理にももっともらしい理由が付いてしまい、
**消す候補が1件も出なくなる**。「不要」のほうが楽な状態にしておく。

引き継いだハーネスには目的が書かれていない。だからこの列は**既にある目的を参照する列ではなく、
目的を処理ごとに組み立てる列**。「このハーネスの目的は?」と白紙で聞く代わりに、
「この処理は何のために在るか」を処理の数だけ聞き、その答えを束ねたものが目的になる。

### ［承認①］処理一覧

**空欄を埋める作業が承認そのもの。** 全行が埋まった時点で承認が成立する。
粒度が違うと以降の図が全部ずれるので、**図より前にここを固める**。

## 2. 処理ごとに理想図を対話で育てる

**「不要」の処理には図を描かない。** 消すものの理想の姿を描く意味がない。
3節の差分表へ直行させる（「この処理を構成する部品を消す」1件）。

### 図は1枚だけ育てる

現状を描く → オーナーと話し合う → **その場で理想へ直す**。**現状図と理想図を2枚描かない。**
2枚残すと、次に引き継いだ人がどちらを契約と読むか分からない
（往復に不動点が無いという失敗が、そのまま再発する）。
経緯は DrillSpark の版履歴に残るので、**最新版が常に理想＝契約**になる。

**理想の出どころは目的だけ。** 判定基準 `reference/harness-design-criteria.md` は
あとから通す検査であって、理想の出どころではない。混ぜると「基準を満たす図」が
「目的を果たす図」を押しのける。

### 手順

1. **プロジェクトを新規に作る**（`create_project`）。処理1つに1つ。名前は `<ハーネス名> / <処理名>`。
   **他人のプロジェクトは読むだけ。** `update_diagram` は全置換で、図は git の外にあり、
   書き戻しに失敗して復旧できるのは作った人だけ。`update_diagram` を使うのは
   **この節で自分が作ったプロジェクトに対してだけ**
2. 実ファイルから読み取れた現状を描く。作図規約は `get_diagram_rules()`
3. **読めなかった箇所もノードとして置き、備考に「実ファイルから読めない。要確認」と書く。**
   図は空欄を描けない（ノードを置かないと線が繋がらない）。
   印を付けずに推測で描くと、**推測が図に定着してそのまま承認される** —
   オーナーは全ノードを疑って読むわけではない。話し合いはこの備考から始める
4. `validate_diagram` で**保存せずに検証する**。ただし**構文しか見ない**（実測）。
   次は通ってしまうので自分で確かめる:
   - **タスクから複数の出力**は規約違反。分岐は decision `{}` から出す
   - **到達不能ノード**（database を後から足すと入力エッジを忘れやすい）
   - 差し戻しの**上限を超えたときの行き先**（上限だけ書いて行き先が無いと上限が無効）
5. 保存し、**読み戻す**（`get_diagram`）。success が返っても内容が巻き戻っていた実例がある。
   壊れていたら `list_versions` → `restore_version`
6. **突き合わせ役（`harness-asis-reviewer`）に照合させる。** 描いた本人は自分の図を疑えず、
   自信を持って間違えた箇所は要確認印では捕まらない。オーナーもこの検査はできない —
   知っているのは目的であって実ファイルの中身ではない。
   渡すのは **project_id と実ファイルの場所だけ**。自分の解釈や要約を渡すと同じ誤読をなぞる。
   指摘は 根拠あり／根拠なし（パス:行が示せない）／検証不能（会話由来）で返る。直すのは自分。
   **2往復で解けない不一致は要確認印に変換して保存し直し**、次の話し合いの材料にする
   （人間ゲートは増やさない。上限だけ書いて行き先が無いと上限は無効）
7. オーナーに URL を出して**2段で話し合う**。**CLI に mermaid を貼らない** — 図はリンクに任せ、
   CLI にはレーンごとの箇条書きで「どの順に何をするか」だけ出す。
   - **1段目＝工程**（並び・分岐・担当・人の止め所）を順に確認しながら、改善案があれば添える。
     案の出どころは目的だけ。案が無い工程は「無し」と言う — 無理に出すと提案のための提案になる
   - **2段目＝詳細**（備考・分岐の条件・成果物・要確認印の箇所）。**工程が固まってから** —
     粒度が動くと詳細は全部描き直しになる（図の承認ゲート①と同じ理屈）。
     要確認印はここで必ず話題にし、印のまま承認へ流さない。
     詳細の途中で工程の間違いに気づいたら、詳細をいじらず工程の確認へ戻る

### 往復は3回で切る

**数えるのは承認ゲート②の差し戻しだけ** — まとめて出して合意に至らなかった回数。
手順7の2段の確認でのやり取りは**数えず、上限も置かない**。毎周オーナーの判断を通るので、
エージェントだけで空回りする危険がない — 上限が守るのは出口のないループであって、対話の長さではない。
小刻みに合意を取っていれば差し戻し自体が減り、3は作業予算ではなく安全弁に戻る。

**3往復で合意に至らなければ、決まらなかった点を⚠オーナー判断として `改善/<日付>.md` に書き残し、
その処理は保留して次の処理へ進む。** 上限だけ書いて行き先が無いと上限は無効なので、
行き先は**すべてオーナー**に統一する（目的が答えられない・処理が切り出せない場合も同じ扱い）。

**保留は失敗ではない。** 1つの処理で止まるとハーネス全体が進まず、全体を見た意味が消える。
**保留と記録されたこと自体が成果**で、次の改善の入口になる。

### ［承認②］理想図

**差分（3節）はこのゲートの材料**として一緒に出す。承認を2回に割らない。
手順7の2段の確認で小刻みに合意していても、**契約になるのはここ** — 全体を1回で見て押す。
重い確認は手順7が済ませているので、**ここがほぼ素通りになるのが正常**。

> 既に `settings.json` から参照されているスクリプトに関わる変更は、**書く前に内容の承認**が要る
> （保存した瞬間から次の発火に効く）。→ harness-implement の「絶対に飛ばさない4点」1

## 3. 処理ごとの差分

**理想図が承認された時点で、実ファイル側が間違っていることは確定する。**
だから「どちらが正か」は差分表に要らない。要るのは**渡し方**。

| 差分の種類 | 理想図のノード | いまの実ファイル | implement へ渡す単位 | 優先度 |
|---|---|---|---|---|

**見るのは構造だけ。散文の言い回しの違いはズレではない。**
文言まで数えると件数が爆発し、承認が「全部まとめて OK」になる。それは承認ではない。

- 図のレーン ↔ `.claude/agents/*.md` の集合
- 図のサブプロセス ↔ skill
- システムレーンの工程 ↔ `settings.json` の hooks・スクリプト実体
- decision の停止点 ↔ `permissions` と skill 本文の停止指示
- 人間ゲートの数と位置

**優先度の列を空にしない。** 無ければ implement 側が順序を勝手に決める。
根拠は「何のために在るか」— 目的への効き方以外に優先度の根拠は無い。

### 処理のループはセッションを分ける

**1セッション＝1処理。同じセッションで2処理目に入らない。**
前の処理の粒度・対話の癖が次の処理の理想図に混入する（implement が
「分離の単位はセッション」とした理由がそのまま当てはまる）。しかも2処理目は
文脈が伸びた状態で始まり、突き合わせと対話の質が一番必要な場面で一番落ちる。

**セッションをまたいで残るのはファイルだけ。** 5節の記録を最後にまとめて書こうとしても、
そのときには前の処理のセッションの記憶が無い。だから**処理を終えるたびに、その処理の節
（理想図リンク・差分・⚠保留）を `改善/<日付>.md` へ追記し、`file-saved-lint.js` で
保存を確かめてから終える**。再開した新しいセッションが読むのは、処理一覧の状態列と
`改善/<日付>.md` だけ。

**終わるときは「次に言う言葉」まで出す**（implement の〈出力の作法〉9 と同じ）。
残っている処理の一覧と、次のセッションでオーナーが言う言葉を出してから終わる。
全処理が終わった回だけ、4節の横断差分へ進む。

**処理の内側で文脈が長くなったら `/compact` で延命してよい** — 処理の中では
同じセッションで描き続ける規律のため、途中で切る選択肢が無い。
**処理の境界では compact ではなく必ず新しいセッション。** compact は要約を持ち越す —
セッションをまたいで残してよいのはファイルだけ。

## 4. 横断差分（全処理が終わってから1枚）

**処理ごとの図を見ていても絶対に気づかないもの**を、ここでだけ出す。

| 見るもの | 出し方 |
|---|---|
| **重複** | 同じ部品が2つ以上の処理の「使っている部品」列に出ている |
| **帰属不明** | 実ファイルに在るのに、どの処理の「使っている部品」列にも出てこない。**先に参照を grep する**（`settings.json` の hooks・permissions、skill / agent 本文からのパス参照、`allowed-tools`）— **参照があるのに処理に出ない部品は削る候補ではなく処理一覧の穴**。`改善/<日付>.md` に「新処理の候補」として記録し、**次の改善の入口**にする — 横断差分は1回だけの規律を守り、この周では戻らない（保留と同じ「記録されたこと自体が成果」の型） |
| **やり方の不一致** | 同じことを処理ごとに別々の方法でやっている |
| **人間ゲートの総数** | ハーネス全体で**オーナーが何回止められるか**。処理ごとに2回×5処理＝10回 |

出せる主張は「どの処理の列にも**出てこない**」まで — 「**使われていない**」ではない。
処理一覧は完全とは限らず、その不完全さはここで露出する。露出したら一覧側を直す。

**帰属不明は報告するだけ。削除はオーナーが決める。** 1回のタスクで使われないのが正常な部品もある。
引き継いだハーネスでは特に、出てこない＝不要ではなく、こちらが踏んでいない場面のための部品かもしれない。
**「なぜ在るのか分からない」は報告の理由であって、削除の理由ではない。**

**差分と一緒に、横断の改善案も出す。** 下限は ECRS（なくす→まとめる→並び替える→簡単にする）で、
**順序が本体** — S を先に出すと不要な作業の高速化になる。材料は上の表そのもの:
帰属不明→E、重複→C、並び→R、やり方の不一致→S。

**ECRS は網であって檻ではない。** 枠は抜け探しの下限で、上限にしない。
枠に収まらない案のための余白を**意図して残してある** — モデルは更新されるので、
今日の枠を明日のモデルの天井にしない。枠外の案は目的から自分で考えて出し、
ECRS のどれでもないことを理由に捨てない。

規律はどの案も同じ: 案の出どころは目的だけ。無ければ「無し」と言う —
無理に出すと提案のための提案になる。**改善案も報告であって決定ではない。** 採否はオーナー。

### 「不要」の処理はここまで実行できない

1節で「不要」と付いた処理の部品を、**必要な処理も使っていることがある**。
どれが共有かを持っているのは**この節の「重複」だけ**。だから順序が決まる:

```text
一覧の承認 → 必要な処理だけ理想図を育てる → 横断差分 → 「不要」行の部品を確定 → implement へ
```

## 5. `改善/<日付>.md` に記録して終わる

**成果物は工程ごとにファイルで残す。** 置き場は `docs/harness/<ハーネス名>/` の下。
雛形はすべて [`reference/設計.md.template`](../../reference/設計.md.template) にある。

| 書く先 | 何を |
|---|---|
| `改善/<日付>.md` | **この工程の成果物。** 処理一覧・理想図のリンク・処理ごとの差分・横断差分・⚠保留 |
| `設計.md`（索引） | 処理一覧と状態列だけ。**無ければ作る**（引き継ぎの既定） |

| 設計書の状態 | どうする |
|---|---|
| 無い（引き継ぎの既定） | **索引 `設計.md` を作る。** 目的が聞ける範囲で埋め、処理一覧と状態列を置く |
| 在る（自作の既定） | **索引の処理一覧と状態列だけを更新する。他は触らない** |

**`処理/<処理名>/合格条件.md` と `実装.md` には絶対に触らない。** 前者は凍結した判定の線で、
満たす側が動かせないことで機能している。「整合させました」の名目で書き換わると裏口から破れる。
**触ってよいファイルを限定することが、凍結を守る唯一の形。**

書いたら**保存を機械で確かめる。**

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/file-saved-lint.js" docs/harness/<ハーネス名>/改善/<日付>.md
```

### 終了条件

**全処理の一覧が承認され、必要な処理の理想図が承認され、横断差分が出て、`改善/<日付>.md` に載ったら終わり。**
**⚠保留が残っていても終えてよい** — 保留ゼロを終了条件にすると、上限を置いた意味が消え、
オーナーに「とりあえず埋めておく」誘因が生まれて保留の記録価値が壊れる。

差分がゼロになるのは implement の仕事の終わりであって、改善の終わりではない。

### 終わるときは「次に言う言葉」まで出す

**改善の出口は implement の入口。** 終わる回は、`改善/<日付>.md` のパスと一緒に次を出してから終わる:

- **採否がオーナー待ちの一覧**（削る候補・改善案・⚠保留）。採否をこのセッションで求めない —
  改善案は報告であって決定ではない
- **採用されたものの行き先**: 処理ごとの差分と改善案は「**新しいセッションで `harness-implement` を
  起動し、優先度の一番高い処理から1つずつ**再適用する」。そのとき言う言葉の例まで書く
  （例: 「`docs/harness/<名>/改善/<日付>.md` の差分表の処理〈◯◯〉を、承認済みの理想図から再適用して」）。
  `settings.json`・`CLAUDE.md`・共有部品に跨るものは `harness-compose` へ

オーナーはAIの初心者かもしれない — 「implement でやって」とだけ言われても
何をすればよいか分からない（implement の〈出力の作法〉9 と同じ理由）。

## 書いてよい場所

**`docs/harness/` 配下と、自分が作った DrillSpark プロジェクトだけ。**
`.claude/` 配下は**読むだけ**。直すのは implement。

> **これは本文のお願いにとどまる。** このハーネス自身が「本文に『必ず通すこと』と書くだけだと破れる。
> 機械が必ず走る形にした」と書いているとおり、柵にはなっていない。確実にするには改善先の
> リポジトリに hook が要るが、**hook が無いのがこの工程の前提**。既知の弱点として残す。

## 人間の介入点

| | 内容 |
|---|---|
| **起動** | オーナーが呼ぶ。**評価から自動連鎖しない** |
| **承認ゲート** | **2つだけ** — ①処理一覧 ②各処理の理想図（差分は②の材料） |
| **実行** | **「何のために在るか」を答えること。** オーナーにしか埋められない |
| **エスカレーション先** | すべてオーナー。3往復超え・目的が答えられない・処理が切り出せない —<br>**全部⚠保留として `改善/<日付>.md` へ**（行き先を1つに統一する） |
| **基準の決定** | **改善は決めない。** 合格条件は implement〈合格条件を決める〉、<br>判定基準は `reference/` のまま |

利用者に見せるときは、この5語に必ず注釈を付ける
→ harness-implement「人間の介入点5種」。**内部IDを利用者向けの文面に出さない。**

## 監査との違い

**理想図の差分（3・4節）は監査ではない。**

| | 何と何を比べるか | 誰が |
|---|---|---|
| **理想図の差分** | **まだ承認されていない**理想図 ↔ 実ファイル | 改善役（この工程） |
| **監査** | **承認済みの**図 ↔ 実ファイル | 評価役 |

区別を消すと、**評価役がまだ承認されていない下書きを基準に判定する**ことになり、
判定役の独立性が壊れる（オーナー判断「監査は評価の工程」はこの線で保たれている）。
承認された瞬間に理想図は契約になり、そこから先の突き合わせは評価役の仕事。
