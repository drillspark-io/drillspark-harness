# 改善計画を1枚にする 実装（2026-09-01）

| 入力にした図 | `updated_at` |
|---|---|
| `process-improve / 改善計画を1枚にする` **7枚**<br>root ／ `2` 材料をそろえる ／ `3` 図を取り寄せる ／ `5` 取れなかった経緯を書き起こす ／<br>`6` 改善案を優先度順に並べる ／ `7` 1枚のHTMLに重ねる ／ `8` 保存して置き場所を伝える<br>**＋ 孤児1枚（キー `4`・59行）** → 4節 #1<br>（プロジェクトUUIDは `_internal/HANDOFF.md`。**公開する docs に書かない** — 規律2） | **2026-08-30T22:55:23Z**<br>2026-09-01 に `list_diagrams` で再確認。**凍結後に図は動いていない**（孤児 `4` も残ったまま） |

**この記録は後追いで書いた。** 実装そのものは **2026-08-31 のセッション**（コミット `1a2baae`）で
〈合格条件を決める〉と同じ回に行われている。2026-09-01 に**実物と `設計.md` §3.3／§3.4 を
突き合わせて**起こした。→ 詳しい経緯と共通の未完は [`../業務を改善する/実装.md`](../業務を改善する/実装.md)。

**この処理の図は、処理1の図を一切渡さない別インスタンスで描き直したもの**（2026-08-30）。
最初の4枚は処理1と同じ文脈で描いており、第一階層に作業を置く／3枚まとめて承認する の2点が破れていた。
破棄した版は `_internal/図/破棄/` にある。

---

## 1. 作ったもの

| 図・ノードID | §3.3 の記載 | 実際はどこになったか | 日付 |
|---|---|---|---|
| 本体レーン（root・`2` `3` `5` `6` `7` `8`） | #2 `skills/process-improve-view/SKILL.md` | 同じ。**3節に畳んだ** — §1 材料をそろえる（図 `2` `3` `5`）／<br>§2 1枚に組み立てる（`6` `7`）／§3 検査して直す（`7` `8`）。<br>レーン名を skill 名に揃える決定（§3.2 決定1）どおり | 2026-08-31 |
| `5` `6` `7` のレーン `書き出し前の検査` | #8 `scripts/process-plan-lint.js` | 同じ。§3 から `業務改善/改善計画.html` に対して呼ぶ | 2026-08-31 |
| `8` のレーン `書き出しの検査` | #9 `scripts/file-saved-lint.js` | 同じ。§3 末尾で保存したパスに対して呼ぶ。<br>**既存の共有スクリプト** — 2026-08-31（4）に `process-file-lint.js` から中立名へ改名 | 2026-08-31 |
| （判定は持たない） | 判定役は #4 `agents/process-improve-reviewer.md` | **この skill は判定しない**（冒頭に明記）。合否は判定役と判定基準ファイルの側にある。<br>実体は処理1側に記録 | 2026-08-31 |
| — | #10 テストの追加 | `tests/ng-plan-*.html` **4件**（`missing-block` / `external` / `no-mark` / `private`）<br>＋ `tests/ok-plan-minimal.html`。`tests/run.sh` に登録済み | 2026-08-31 |

**§3.4 #10 がこの処理の要の条件** — `ok-plan-minimal.html` が **exit 0**。
既存の `harness-view-lint` はここで落ちる（DrillSpark の URL に含まれる UUID を私的情報として弾くため）。
**1枚から図へ戻る導線を消さない**ことを、テストで縛っている。

**§3.4 #15** で `PRIVATE_INFO`（絶対パス・メール）を後から足した。この1枚は一般公開ツールの出力で、
**そのまま社内で共有される**。凍結後の変更だが**厳しくする向き**なので凍結の趣旨に反しない。

---

## 2. 統合へ渡す仕様

| 種別 | 仕様 |
|---|---|
| hook 定義 | **無し**（処理1と同じ。§3.2 決定4） |
| 権限 | **無し**（§3.2 決定5）。⚠ `allowed-tools` の MCP 名不一致は処理1と共通 |
| 常時オンにしたい指示 | **無し** |

---

## 3. ⚠ 推測

**この処理に固有の推測は無い。** 共通の1件（教訓の上限10行）は
[`../業務を改善する/実装.md`](../業務を改善する/実装.md) 3節。
**後追い記録なので、実装時の推測を網羅できている保証は無い**のも同じ。

---

## 4. 未完

1. ~~孤児の図が1枚残っている — キー `4`（59行）~~ → **2026-09-01 に削除済み**（オーナー承認）。
   `delete_diagram`（claude.ai コネクタ版にはある。HANDOFF の「MCP に無い」は誤り）で消し、
   `list_diagrams` で**残り7枚＝確定分ちょうど**を確認した。削除前スナップショットは版履歴にある。
   ⚠ **root の `class ... drillDown` に `4` が残る** — 見た目だけの残骸。→ [`図.md`](図.md)
2. ~~`図.md` と `合格条件.md` が新形式になっていない~~ → **2026-09-01 に移行済み**
   （[`図.md`](図.md) ／ [`合格条件.md`](合格条件.md)。中身は動かしていない）。
   ⚠ **図の備考の逐語は移せていない**のは処理1と共通
3. **`MAPPING.md` に「外部サービス」のレーン分類が無い。** この処理の図に出た
   `DrillSpark（外部サービス）` は人間／agent／システムのどれでもなく、**対応表の行き先が空欄**になる。
   → `harness-implement` 側の穴。この設計書ではなく HANDOFF の未解決に置いてある
4. **〈評価する〉未実行**
