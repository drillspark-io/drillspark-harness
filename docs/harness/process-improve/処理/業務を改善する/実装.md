# 業務を改善する 実装（2026-09-01）

| 入力にした図 | `updated_at` |
|---|---|
| `process-improve / 業務を改善する` **8枚**<br>root ／ `2` 一覧を作る ／ `3` 業務を選ぶ ／ `4` 図にする ／ `4_3` 専門家役が工程の案を出す ／<br>`5` 改善案を出す ／ `5_4` 業務改善コンサルが問いかける ／ `6` AI化依頼書を作る<br>（プロジェクトUUIDは `_internal/HANDOFF.md`。**公開する docs に書かない** — 規律2） | **2026-08-30T22:27:33Z**<br>2026-09-01 に `list_diagrams` で枚数と行数を再確認。**凍結後に図は動いていない** |

**この記録は後追いで書いた。** 実装そのものは **2026-08-31 のセッション**（コミット `1a2baae`）で
〈合格条件を決める〉と同じ回に行われ、成果物ファイルだけが残っていた。
2026-09-01 に**リポジトリの実物と `設計.md` §3.3／§3.4 を突き合わせて**この1本に起こしている。
**実装時の判断のうち `設計.md` に印が残っていないものは復元できない**（→ 3節）。

**入力**: [`図.md`](図.md)（8枚）と [`合格条件.md`](合格条件.md)（2026-08-31 凍結）。
両方とも 2026-09-01 に `設計.md` から新形式へ移した（**中身は動かしていない**）。
両処理にまたがる分（機械抽出・共通の決定・条件 #13 #14・凍結の宣言と解き方）は `設計.md` §3 に残る。

---

## 1. 作ったもの

**`設計.md` §3.3 の一覧と実物を両方向で突き合わせた。ズレは無い。**
`設計.md` §3 は凍結されているので、以後ズレが出たらこの表に実物を書く（あちら側を直さない）。

| 図・ノードID | §3.3 の記載 | 実際はどこになったか | 日付 |
|---|---|---|---|
| 本体レーン `process-improve`<br>（root・`2`〜`6` の6枚） | #1 `skills/process-improve/SKILL.md` | 同じ。5工程が §1〜§5、疎通確認が「始める前に — DrillSpark に繋がるか確かめる」 | 2026-08-31 |
| `4_3` `5_4` のレーン `process-expert` | #3 `agents/process-expert.md` | 同じ。`Task` で2回呼ぶ — §3（工程の案出し）と §4（業務改善コンサル）。**役割は起動時に渡す汎用1本** | 2026-08-31 |
| **図に無い**（判定は図の外） | #4 `agents/process-improve-reviewer.md` | 同じ。§5 末尾で `Task → process-improve-reviewer`。<br>**処理2からも判定役として参照される**（`process-improve-view` は判定を持たない） | 2026-08-31 |
| 全図 Start の備考「基準の決定＝判定基準ファイル」 | #5 `reference/business-improvement-criteria.md` | 同じ。**両処理が読む共通物。**利用者も AI も決めない | 2026-08-31 |
| 成果物ノード（業務一覧・改善案・AI化依頼書・保留） | #6 `reference/business-improvement-tables.md` | 同じ。§3.6 で列が2つ増えた版（`聞いた相手` ／ `出どころ`） | 2026-08-31 |
| `2` `6` のレーン `記入漏れの検査` | #7 `scripts/process-table-lint.js` | 同じ。**本文から2回呼ぶ** — §1 末尾（`業務改善/業務一覧.md`）と §5 末尾（`業務改善/AI化依頼書.md`） | 2026-08-31 |
| `2_9`（業務一覧）／`4_5`（工程）／`4_8`（作業） | §3.5 **承認ゲート3つ** | `SKILL.md` の `【止まる①】業務一覧の確認` ／ `【止まる②】工程の確認` ／ `【止まる③】作業の確認`。<br>出し方は `AskUserQuestion`（「出力の作法」に明記） | 2026-08-31 |
| — | #10 テストの追加 | `tests/ng-table-*.md` **8件** ＋ `tests/ok-table-minimal.md`。`tests/run.sh` に登録済み。**既存16件には触れていない** | 2026-08-31 |
| — | #11 `.claude-plugin/plugin.json` ／ #12 `README.md` | **両処理で1回。**`agents` に2本追加、`description` と `keywords` を業務改善へ拡張、README に構成表と lint の節 | 2026-08-31 |

**合格条件の結果**（`設計.md` §3.4 の18件をそのまま実行・2026-09-01 再実行）

```text
bash tests/run.sh                 → 全件が期待どおり（lint 32件 ＋ 同梱ファイル 23件）
claude plugin validate . --strict → passed
```

**⚠ §3.4 #14 は同梱の存在チェックを「13 → 22件へ」と書いているが、実際は 23件。**
条件は「全件 PASS」なので**不合格ではない**が、括弧内の数が実態と1件ずれている。
`設計.md` §3 は凍結されているので**あちらを直さず、ここに実物を書く**。

---

## 2. 統合へ渡す仕様

**この処理では `settings.json` と `CLAUDE.md` を書かない。**書くのは `harness-compose` の1箇所だけ。

| 種別 | 仕様 |
|---|---|
| hook 定義 | **無し。** `設計.md` §3.2 決定4 — 一般公開ツールが利用者の `settings.json` を書き換えない。<br>検査は **skill 本文から呼ぶ**。**本文のお願いなので破れうる**（既知の弱点として引き受けている） |
| 権限 | **無し。** §3.2 決定5 — 利用者の権限設定を触らない。`allowed-tools` で宣言するだけ。<br>⚠ その `allowed-tools` に既知欠陥あり（→ 4節 #2） |
| 常時オンにしたい指示 | **無し。** 教訓の置き場は利用者の `業務改善/`（§3.2 決定6）で、プラグイン側の `CLAUDE.md` には出さない |

**→ この処理が統合へ渡すものは実質ゼロ。** `harness-compose` が束ねる3つ
（`settings.json` の hook と権限・`CLAUDE.md`・合格条件）のうち、**合格条件だけが対象**になる。
一般公開ツールとして利用者環境を触らない決定の帰結であって、書き漏らしではない。

---

## 3. ⚠ 推測

| # | 推測した箇所 | 出どころ |
|---|---|---|
| 1 | **教訓の上限「最大10行・超えたら統合してから追記」** — 図にも指示にも上限が無かったので `harness-implement` の規約に揃えた | `設計.md` §3.2 末尾に印が残っている |

**この後追い記録の限界**: 上の1件以外に実装時の推測があったかは**復元できない**。
実装したセッションの会話は残っていない。〈評価する〉は**実物と §3.3／§3.4 の突き合わせ**でしか見られない。

---

## 4. 未完

1. ~~`図.md` と `合格条件.md` が新形式になっていない~~ → **2026-09-01 に移行済み**（オーナー承認）。
   凍結内容は動かさず場所だけ移した。⚠ **図の備考の逐語は移せていない** — 後追いのため
   `図.md` の介入点表はノードIDまでで、備考そのものは DrillSpark にしかない
2. **`allowed-tools` の MCP 名が環境によって一致しない。** `mcp__drillspark__*` と宣言しているが、
   claude.ai コネクタ版は `mcp__claude_ai_DrillSpark__*`。壊れないが事前許可が効かない。
   `plugin.json` の `mcpServers` でサーバーを宣言すれば決定的になる — **未着手**（既存3スキルと同じ欠陥）
3. **直しの通算回数（上限3回）が記録されていない。** レビュー役の指摘は `設計.md` §3.6 に
   **4件**（合格条件 #16 #17 #18 の追加と、表の列2つ）として痕跡が残っているが、回数そのものは残っていない。
   後追いでは数え直せない
4. **〈評価する〉未実行。** `harness-evaluator` はこの `実装.md` を入力にする
